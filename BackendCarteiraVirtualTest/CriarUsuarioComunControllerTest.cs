using BackendCarteiraVirtual;
using BackendCarteiraVirtual.Controllers;

namespace BackendCarteiraVirtualTest
{
    [TestClass]
    public class CriarUsuarioComunControllerTest
    {
        [TestMethod]
        public void TestMethod1()
        {
            // Arrange
            var controller = new CriarUsuarioComunController(null);

            // Act
            //var result = controller.Post(paramter);

            // Assert
            //Assert.IsNotNull(result);
        }
    }
}